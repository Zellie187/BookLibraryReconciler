# Dev tools
pytest>=8.0
ruff>=0.6
black>=24.0
mypy>=1.10

# Report Engine - only Excel/PDF output need these (CSV/JSON/HTML are
# stdlib-only); imported lazily, so the rest of the app runs without them
openpyxl>=3.1
fpdf2>=2.7

# Cover Download Engine - cover image validation/resizing; imported lazily,
# so the rest of the app runs without it
Pillow>=10.0

# GUI (`python run.py gui`) - imported lazily, so the CLI runs without it
PySide6>=6.5
